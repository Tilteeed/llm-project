- name: Configure LLM nodes
  hosts: llm_nodes
  become: true
  roles:
    - { role: common, tags: ["common"] }
    - { role: docker, tags: ["docker"] }
    - { role: llm, tags: ["llm"] }
    - { role: monitoring, tags: ["monitoring"] }
